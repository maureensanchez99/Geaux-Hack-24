@inject c_currentUser c_currentUser



<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Pocket Travel</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="profile">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Profile
            </NavLink>
        </div>

        <nav>
    <div class="nav-item px-3">
        @if (!c_currentUser.IsLoggedIn)
        {
            <NavLink class="nav-link" href="login">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Login
            </NavLink>
        }
        else
        {
            <span>adwadw</span>
        }
    </div>

    @if (c_currentUser.IsLoggedIn)
    {
        <div class="nav-item px-3">
            <span class="nav-link">Welcome, @c_currentUser.fullName!</span>
        </div>
    }
</nav>
    </nav>
</div>

